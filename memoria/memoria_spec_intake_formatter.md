# Memoria — Spec Intake Formatter

> Este fichero es la memoria **única y compartida** que el agente lee al inicio de cada sesión
> (tras un `pull`) y actualiza al final (antes de un `push`). La usan todos los compañeros que
> trabajan con este agente: no hay un fichero de memoria por usuario. Cada entrada debe
> identificar quién la registró en la columna "Usuario" para mantener trazabilidad.
>
> Solo debe contener información procedente de los documentos fuente o de respuestas literales
> del usuario — nunca conocimiento propio del modelo. No lo edites manualmente salvo para
> corregir un error; deja que el agente lo mantenga.

## 1. Glosario y convenciones de la aplicación

| Término | Definición | Usuario | Fecha |
|---------|------------|---------|-------|
| SSI | Standing Settlement Instruction — instrucción de liquidación estándar | pablo.llorente | 2026-09-16 |
| RDR | Sub-aplicación propietaria de las cadenas analizadas | pablo.llorente | 2026-09-16 |
| ANS RDR | Grupo de soporte responsable (BZG03906, ans_rdr.es@bbva.com) | pablo.llorente | 2026-09-16 |
| UUAA | Código de aplicación BBVA (p.ej. KYTL0000) | pablo.llorente | 2026-09-16 |
| Control-M | Herramienta de orquestación de cadenas (Server MERCADOS-4) | pablo.llorente | 2026-09-16 |
| Planificador Genérico | Motor Java (ProjectMain.jar) que ejecuta SQL y genera ficheros CSV/TXT/XML según tablas FT_T_ATE1/QPF1/PAR1 | pablo.llorente | 2026-09-17 |
| GoldenSource | Base de datos Oracle (BKYTL003 @ LDORA605:1525, usuario KYTL_GC) con tablas maestras ft_t_* | pablo.llorente | 2026-09-17 |
| Legal Agreement | Contrato Marco de BBVA SA; tabla maestra FT_T_LAGR en KYTL_GC | pablo.llorente | 2026-09-22 |
| Job Dummy | Job de Control-M sin efecto funcional que solo informa el fin de una rama | pablo.llorente | 2026-09-22 |
| Pasarela (LPFTP501/502) | Máquina intermedia para envíos SFTP a terceros externos a la red BBVA | pablo.llorente | 2026-09-22 |
| IHS Markit | Destino externo (SFTP-PROD.CAPPITECH.COM), único consumidor fuera de la red interna | pablo.llorente | 2026-09-22 |
| SAIT | Aplicativo consumidor **exclusivamente mexicano** (máquina 150.100.230.96, contacto bex-sait.group@bbva.com) | pablo.llorente | 2026-09-22 |
| DataX | **Plataforma de transferencia, NO un sistema consumidor.** RDR solo disponibiliza el fichero en `/unload/kytl/datsal/datax` y es el sistema destino quien monta la transferencia. Ver `memoria/memoria_datax_RDR.md` | pablo.llorente | 2026-09-22 |
| DataObject | Identificador estable de un fichero dentro de DataX (`x_*`). Es la referencia por la que preguntar ante una incidencia de entrega, no la ruta ni el nombre del fichero en destino | pablo.llorente | 2026-09-22 |
| 1145 | Código de la sucursal de México en `FT_T_ENTR.ORG_ID` / `FT_T_CNTA.ORG_ID`. Aparece como `AgreementORGID` en sait.xsl y como "sucursal activa 1145" en Altamira/Bancomer México | pablo.llorente | 2026-09-22 |
| ORG_ID | Identificador de organización en `FT_T_ENTR`, referenciado desde `FT_T_CNTA`. **Columna de ancho fijo**: los literales llevan relleno (`'A15 '`, `'1145'`). Comparar sin `TRIM` exige respetar el padding | pablo.llorente | 2026-09-22 |
| FT_T_CNTA | Asignaciones de un contacto. `CONTCT_ASSIGN_STAT_TYP` distingue el tipo: `BRANCH` (sucursal), `FUNCTION` (función), `INSTIT` (institución) | pablo.llorente | 2026-09-22 |
| FT_T_ATE1 (uso en ExtraccionGenericaOtherEntities) | Además de configurar el Planificador Genérico, almacena las **queries SQL** que ejecuta el jar, identificadas por la columna `ACTION_NME` | pablo.llorente | 2026-09-22 |
| MyThreadCpty | Clase del pool de hilos que paraleliza la query de detalle, una ejecución por entidad | pablo.llorente | 2026-09-22 |
| @@ENV@@ | Token de los `.properties` sustituido en despliegue por el entorno: pr, pp, ei, de | pablo.llorente | 2026-09-22 |

Entradas adicionales (registradas en formato lista en la rama `feature/Eduardo`):
- **CADF (Calendars):** entidad RDR que cataloga días operativos/no operativos por mercado o divisa. _(pablo.llorente, 2026-09-17, Envío de Calendarios a Modelity)_
- **KYTL:** aplicación asociada al proceso de envío de calendarios a Modelity, y también al de Envío a Altamira Colombia (P-035) — es una app compartida por varias cadenas RDR. _(pablo.llorente, 2026-09-17)_
- **GoldenSource:** origen de datos (tablas `FT_T_CADF`, `FT_T_CADP`, `FT_T_MRKT`) desde el que se extrae `Calendarios.csv`. `MARKET_CODE`/`CAL_ID` son campos internos de este modelo y no viajan en el fichero físico. _(pablo.llorente, 2026-09-17)_
- **RNUM:** correlativo de fila dentro de `Calendarios.csv`; no forma parte de la clave de negocio. _(pablo.llorente, 2026-09-17)_
- **Criticidad W:** nivel de criticidad de job en Control-M = aviso al día siguiente (no inmediato). _(pablo.llorente, 2026-09-17)_
- **Escala completa de criticidad (fichas EX-005-03):** W = aviso día siguiente; S = aviso día siguiente incluso si es festivo; C = aviso inmediato. Complementa la entrada anterior. _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- **ODATE+2:** desplazamiento de la fecha de proceso dos días, usado cuando el calendario del sistema destino no coincide con el de la cadena (p. ej. la variante sabatina de la rama IHS Markit). _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- **Convención general:** el diccionario de datos "de alto nivel" de un documento de análisis puede no coincidir con la estructura física real del fichero — verificar siempre contra el CSV/fichero real antes de dar por buena una especificación de campos. _(pablo.llorente, 2026-09-17)_
- **RAMERC0068.sh:** script genérico de manipulación de ficheros (copia, chown, historificación) reutilizado en varias cadenas RDR (Calendarios, Altamira Colombia, Altamira/Bancomer México), parametrizado por `PARM1`. _(pablo.llorente, 2026-09-18)_
- **DataX / datax-agent:** plataforma e infraestructura de transmisión de ficheros distinta de RDR (host `datax-live`, servidor Control-M `MERCADOS-1`), con cuentas de ejecución propias (p. ej. `epsilon-ctlm`) ajenas a las cuentas RDR habituales (`xakytl1p`, `xsramer1`, etc.). _(pablo.llorente, 2026-09-18, Envío a Altamira/Bancomer México)_
- **ODATE:** variable de fecha operativa de Control-M; el mismo valor puede requerir formateo distinto según el parámetro/sistema destino que la consuma (p. ej. `YYYYMMDD` vs `AAMMDD` en un mismo comando `datax-agent`). _(pablo.llorente, 2026-09-18, Envío a Altamira/Bancomer México)_
- **P-021 (Carga y conciliación de datos de clientes BDI):** sistema RDR compuesto por 8 cadenas Control-M independientes (`RDR_BANCARIZACION_new`, `RDR_CARGA_BAJA_NIVELES_new`, `RDR_CLIENTES_CIB_new`, `RDR_CONCILIACION_BDI_new`, `RDR_CONCILIACION_CLIENTELA_new`, `RDR_ENVIO_CLIEX_new`, `RDR_PR_BDICLIENREG_RESP_new`, `RDR_REFUNDICION_new`), 48 pasos en total. Cada cadena se especifica como proceso independiente en `salidas/`. _(pablo.llorente, 2026-09-21)_
- **GSProcess.sh / `.properties`:** motor genérico de KYTL que, según el `PARM1` inyectado, carga un fichero `.properties` distinto (p. ej. `bajaniveles.properties`, `ConBDI.properties`, `Refundicion.properties`) para parametrizar banderas (Delta, Preprocesado, MDX, Workflow, Errores, Reporte), disparar workflows de GoldenSource (`Accion=Evento`/`NomWorkflow`) y post-procesar con scripts (`Unix2Dos`, `QuitarNulos`, etc.). Patrón reutilizado en todo el sistema P-021. _(pablo.llorente, 2026-09-21)_
- **PLSQL_Load:** workflow genérico de GoldenSource (motor de ingesta asíncrona por lotes de 500 registros, delega en `Sub_Load`) usado por varios procesos de refundición/carga bajo el patrón `<Proceso>.properties` → `<Proceso>.gsp` (evento) → `PLSQL_Load` (workflow). _(pablo.llorente, 2026-09-21, RDR_REFUNDICION_new)_
- **Un `LISTAGG` correlacionado por combinación (no por entidad individual) puede convertir "una fila = un registro" en "una fila = varios registros concatenados", sin que la documentación funcional lo refleje.** En `Querys.obtenerIDs()` de Altamira México, cada línea del fichero final corresponde a una combinación institución/sucursal, no a un ALID — si esa combinación tuviera más de un ALID asociado, la línea llevaría varios códigos pegados con `|`. Es un riesgo distinto (aunque relacionado) del de duplicidad entre filas: aquí el problema es la agregación DENTRO de una fila, no la repetición ENTRE filas. Cuando una query usa `LISTAGG`/`STRING_AGG` para producir el valor de una columna que se documenta como "un valor simple", verificar siempre la cardinalidad real de la correlación antes de asumir que siempre habrá un único elemento agregado. _(pablo.llorente, 2026-09-24, Envío a Altamira/Bancomer México)_
- **Un `SELECT DISTINCT` no siempre deduplica al nivel que parece a simple vista.** En `Querys.obtenerIDs()` de `AltamiraMexicoConciliacion.jar`, el `DISTINCT` envuelve el resultado de un `LISTAGG(DISTINCT fiid.fins_id,'|')` correlacionado por combinación institución/sucursal — deduplica cadenas concatenadas completas, no el ALID individual. Un mismo ALID vinculado a 2 sucursales distintas genera 2 filas de resultado distintas (cada una con su propia cadena `LISTAGG`), y el `DISTINCT` no las fusiona. Contraste directo con el caso análogo de Colombia (`SELECT DISTINCT FINS_ID` sobre la columna atómica), donde sí deduplica de verdad — mismo patrón de pregunta ("¿hay duplicados?"), mecanismo real opuesto en cada jar. Nunca asumir que dos queries con "DISTINCT" en el texto se comportan igual sin mirar sobre qué expresión exacta actúa. _(pablo.llorente, 2026-09-24, Envío a Altamira/Bancomer México)_
- **Antes de aceptar "solo JAR compilado, sin fuente" como límite definitivo de evidencia, volver a preguntar cuando el usuario ya ha demostrado poder aportar fuente descompilada de otras clases del mismo jar.** En `RDR_ConciliaColombia.jar`, el usuario ya había aportado antes clases decompiladas (`Querys.executeCON_Hilos`, hallazgo del defecto de pérdida de lote, no documentado por decisión suya). Al retomar el pendiente de duplicados, la misma vía (pedir el código fuente/decompilado) funcionó: `ColombiaEnvio.java`+`Querys.java` mostraron que `obtenerIDs()` usa `SELECT DISTINCT`, cerrando el gap sin ambigüedad. _(pablo.llorente, 2026-09-24, Envío a Altamira Colombia)_
- **`MAXRERUN` (atributo de job) y `DOACTION RERUN` (regla `ON`/`DOACTION`) son 2 mecanismos independientes de Control-M, no contradictorios entre sí.** El primero gobierna el reintento automático ante ABEND/NOTOK a nivel de job (aquí en `0`, desactivado); el segundo es una regla condicional (`ON CODE="COMPSTAT EQ 7" → DOACTION RERUN`) sin contador propio en el modelo de Control-M — se dispara cada vez que se cumple la condición, sin límite numérico configurado. Cuando el límite de un `DOACTION RERUN` parece "no especificado", buscar si el límite real es temporal (ventana horaria del job) en vez de un contador: en `KYTL_CAL_MODELITY_FW`, el timeout interno del propio `ctmfw` (60 min) coincide exactamente con la ventana horaria del job (22:00-23:00, confirmada por su ficha real), así que el `RERUN` solo puede dispararse una vez antes de que la ficha exija KO por fin de ventana. _(pablo.llorente, 2026-09-24, Envío de Calendarios a Modelity)_
- **`FT_T_ATE1.DATA_STAT_TYP` no gatea el `SELECT` por `ACTION_NME` en el motor `ExtraccionGenericaOtherEntities`.** Confirmado con el código fuente real de `Querys.java`: los 3 métodos que buscan una acción por `ACTION_NME` en `FT_T_ATE1` (`obtenerEntidades`, `obtenerExtraccion`, `obtenerFichero`) no incluyen ninguna condición sobre `DATA_STAT_TYP`; ese campo sí se usa como filtro, pero solo en las queries contra `FT_T_PAR1` (etiquetas raíz), nunca sobre `FT_T_ATE1`. Por eso `ExtraccionCONT.sql` sigue funcionando con normalidad pese a estar marcada `INACTIVE` desde 15-SEP-25 — el campo no es un mecanismo real de activación/desactivación para este motor, pese a lo que su nombre sugiere. Aplicable a cualquier otro proceso que use el mismo jar (`ExtraccionGenericaOtherEntities.jar`/clase `Querys`), como Contratos BBVA. _(pablo.llorente, 2026-09-24, RDR_EXTRACCION_CONTACTOS)_
- **`MEGENV0001.sh`:** script genérico de envíos/recogidas (XCOM, Connect Direct, SFTP) reutilizado por decenas de jobs en todo el proyecto RDR, invocado siempre como `MEGENV0001.sh <NOMBRE_JOB>` (la propia clave del job identifica su `.idx`/config de parámetros). **Confirmado con script real (2026-09-24):** la variable `FALLA_NO_FICHERO` controla el comportamiento ante ausencia de fichero — `"SI"` produce fallo real del script (distintos códigos según el punto exacto: `60` si la máscara de envío no encuentra ningún fichero en sentido PUT/MPUT, `45` si el fichero concreto falta tras pasar la máscara, `98`/`96`/`97` en el lado de recogida GET/MGET); `"NO"` solo registra un aviso en el log y continúa (Soft Failure). Este es el mecanismo genérico detrás de todas las asimetrías "estricto/Soft Failure" ya documentadas en varios procesos del proyecto (`RDR_ENVIO_CLIEX_new`, `RDR_CONCILIACION_CLIENTELA_new`, `RDR_REFUNDICION_new`, `RDR_PR_BDICLIENREG_RESP_new`) — todas consistentes con este único script, no comportamientos ad-hoc de cada job. _(pablo.llorente, 2026-09-24, RDR_ENVIO_CLIEX_new)_
- **RDR_Validacion_XSD.sh:** script genérico de validación XSD reutilizado por varios jobs de una misma cadena (parametrizado `$1`=entorno `de/ei/pp/pr`, `$2`=tipo: `CPARTY`/`BASKET`/`ISSUE`/`ISSUERESTO` implementados, `CONTACT`/`CONTRACT_BBVA`/`CONTRACT_BANCOMER` reconocidos pero no implementados). **Confirmado con script real (2026-09-24):** troceado `awk` en bloques de 1.000 registros, validación `xmllint --schema` en paralelo (máx. 20 procesos vía `jobs -r`/`wait -n`), XSD por tipo en `/$ENV/kytl/online/multipais/multicanal/dat/properties/` (`RDR_XSD_Generico.xsd`/`Baskets_Schema.xsd`/`xsd_emisiones_batch.xsd`, este último compartido por `ISSUE` e `ISSUERESTO`). **Hallazgo:** el script solo falla (`exit 1`) ante un desbalance estructural de etiquetas (chequeo previo `estructura_xml()`); una violación real de XSD detectada por `xmllint` solo se registra en el log, nunca hace fallar el script (siempre `exit 0`) — ver lección de estructuración más abajo. _(pablo.llorente, 2026-09-22 / 2026-09-24, Carga multipetición Refinitiv emisores / RDR_ISSUES_RE_PRO_new)_
- **Criticidad de cadena vs. criticidad de job:** una cadena puede declarar una criticidad global (p. ej. "A - Aviso inmediato") distinta de la de sus jobs individuales ("C"/"W"); la de cadena actúa como regla de escalado adicional (p. ej. "si cualquier job C falla, notificar de inmediato a nivel de cadena"), no como un reemplazo de la criticidad por job. _(pablo.llorente, 2026-09-22, RDR_ISSUES_RE_PRO_new)_
- **Placeholder "RA" / "A definir por RA":** valor literal encontrado en el campo "Librería Origen" de varias fichas técnicas del mismo documento — confirmado por el usuario como placeholder de documentación sin resolver en la fuente, sin significado funcional ni impacto en la ejecución real. _(pablo.llorente, 2026-09-22, RDR_ISSUES_RE_PRO_new)_

## 2. Respuestas reutilizables del usuario
| Tema | Respuesta literal | Usuario | Fecha | Proceso |
|------|--------------------|---------|-------|---------|
| Protocolo ante fallo de cadena | "La cadena falla y se para" | pablo.llorente | 2026-09-17 | RDR_DICTIONARY_INDEX_new |
| Envío a destino (MEKYTL0860) | "El envío no puede fallar; en todo caso fallará su recepción" | pablo.llorente | 2026-09-17 | RDR_DICTIONARY_INDEX_new |
| Ficha semanal DORMIDA | "Seguramente lo haga el planificador genérico, lo que pasa que estará inactivo y no se esté generando" | pablo.llorente | 2026-09-17 | RDR_FIC_DAT_DICT_WEEKLY_SEND_new |
| Clave de negocio del fichero de calendarios (nivel BBDD) | "la clave única lógica real debe ser la composición de tres campos: MARKET_CODE + CAL_ID + CALENDAR_DATE" | pablo.llorente | 2026-09-16 | Envío de Calendarios a Modelity |
| Estructura real de Calendarios.csv | "CAL_ID NO viaja físicamente en el fichero... 4 columnas: CURRENCY;CAL_DAY;HOLIDAY;RNUM" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Control de duplicados | "la detección y prevención de duplicados... se delega 100% a la lógica de la aplicación (GoldenSource/ETL) o a la propia consulta SQL" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Fallback viernes festivo (CSCF/TFIT) | "Se envía igualmente, ya que se supone que se verá en el próximo día lectivo" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Contenido de HOLIDAY | "Es un ENUM cerrado de exactamente 2 valores: WEEKEND, HOLIDAY... 0 registros con valor vacío... 0 registros marcados como BUSINESS_DAY" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Criterio de completitud del fichero | "No se debe validar la continuidad secuencial de fechas... el fichero contenga registros dentro del rango temporal esperado... 87 divisas únicas" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Fallos de transferencia | "Aviso por correo y código de error... RAMERC0068.sh... sin realizar reintentos automáticos" | pablo.llorente | 2026-09-16 | Envío de Calendarios a Modelity |
| Historificación | "Último paso secuencial... no definen una lógica condicional que detenga o fuerce la historificación ante un fallo previo" | pablo.llorente | 2026-09-16 | Envío de Calendarios a Modelity |
| Criticidad W vs. impacto downstream | "Criticidad W confirmada en fichas... el posible desajuste de riesgo debe ser evaluado directamente con los responsables de negocio" | pablo.llorente | 2026-09-16 | Envío de Calendarios a Modelity |
| Ruta origen del Salto 2 (MEKYTL1044_SND) | "MEKYTL1044_SND realmente lee de /fichtemcomp/.../send/ en lpftp503" — confirmado contra la definición real en Control-M; existe una réplica real de esa estructura de directorios en lpftp503, independiente de /unload/transmisiones/KYTL/ | pablo.llorente | 2026-09-17 | Envío a Altamira Colombia |
| Rango temporal de Calendarios.csv (evidencia de muestra, no regla confirmada) | Fichero real de producción analizado: 251.874 filas, rango CAL_DAY 2022-09-21 a 2049-12-31 (~27 años y 3 meses), festivos reales (no solo WEEKEND) poblados en profundidad (~770-1020/año) en todo el rango. Usuario: "no lo sé con certeza [si 2049-12-31 es fijo o relativo], haz eso y déjalo como evidencia de la muestra" | pablo.llorente | 2026-09-17 | Envío de Calendarios a Modelity |
| Rango temporal de Calendarios.csv — resuelto | Cruzando una consulta real contra `FT_T_CADP` con el fichero real de producción: el máximo por divisa es un valor estático precargado (heterogéneo por divisa, no un único "2055"), y el fichero exportado lo recorta exactamente 6 años (mismo mes/día) respecto al máximo real de la tabla. Confirmado por el usuario: "el recorte de 6 años... es deliberado... para evitar que los sistemas consumidores aguas abajo (XERG, BONT, Mentor, CSCF, etc.) interpreten los años 2050-2055 como el siglo pasado (1950-1955)" | pablo.llorente | 2026-09-24 | Envío de Calendarios a Modelity |
| Naming real del fichero de Altamira México | "El nombre real en código es RDR_clientesYYYYMMDD.csv (con guion bajo y sin espacios), definido en Ficheros.sacarFichero vía ArgJava4. Las variantes de MEKYTL1205 son erratas de redacción" | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| MEKYTL1205 Run As | "Usuario (Ejecutar como): xsramer1... ejecuta físicamente /pr/pl/scrt/RAMERC0068.sh en MERCADOS-4" | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| MEKYTL1221 evento de salida y rearranque | "MEKYTL1221 sí agrega un evento de salida: RDR_ALTAMIRAMEX_SEND_MEKYTL1221_OK" pero "Control-M dará la cadena por terminada con éxito aun si la transmisión a DataX falla"; sin regla de rearranque propia, aplica la regla por defecto del folder (ANS RDR) | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| Estructura real de RDR_clientesYYYYMMDD.csv | "La implementación Java posterior descompone esa cadena con split(\"\\|\") y escribe cada código en una línea independiente... CSV delimitado por ;, cabecera, un código por línea" | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| Discrepancia query envío vs. conciliación | "La query obtenerCLIs... no aplica el filtro de sucursal activa 1145 ni excluye los 5 códigos hardcodeados... confirmada como inconsistencia/riesgo" | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| Alcance de la conciliación | "La cadena no pertenece al folder Control-M KYTL0000-RDR_ALTAMIRAMEX_SEND. Debe quedar fuera del alcance de la orquestación de este proceso" | pablo.llorente | 2026-09-18 | Envío a Altamira/Bancomer México |
| Independencia Batch vs. Multi | "Son integraciones funcionalmente independientes. La cadena Batch (21:00h) ejecuta la extracción periódica masiva diaria, mientras que la cadena Multi (cíclica cada 45 min) procesa peticiones incrementales de novedades vía fichero plano" | pablo.llorente | 2026-09-22 | RDR_BATCH_EMISORES_REFINITIV / RDR_CARGA_REFINITIV_Multi |
| Ausencia de validación de contenido de respuesta Refinitiv | "La malla no realiza inspección o parsing del contenido de la respuesta de Refinitiv; únicamente valida el código de salida de éxito (Return Code 0)" | pablo.llorente | 2026-09-22 | RDR_BATCH_EMISORES_REFINITIV |
| Borrado condicionado al éxito de transferencia (MEKYTL1058) | "La instrucción de borrado... se ejecuta secuencialmente tras la confirmación exitosa de la transferencia a LPRDR501" | pablo.llorente | 2026-09-22 | RDR_CARGA_REFINITIV_Multi |
| MEKYTL0981 es el colector real del bloque Resto | "MEKYTL0981 actúa técnicamente como el colector final del bloque 'Resto' (23:00h)... El texto que lo asocia a 'ReportingEngine' es una inconsistencia documental/heredada en la descripción" | pablo.llorente | 2026-09-22 | RDR_ISSUES_RE_PRO_new |
| Semántica de baja en Sub_BajaCpartiesGL | "La baja es lógica y no física. El sub-workflow actualiza DATA_STAT_TYP = 'INACTIVE'... y conserva trazabilidad de auditoría" | pablo.llorente | 2026-09-23 | RDR_CARGA_BAJA_NIVELES_new |
| Motivo de jobs "A DUMMY" (MEKYTL0352 / MEKYTL0135) | "En ambos casos se trata de jobs dummy de control cuya función es solicitar una transmisión XCOM", sin motivo de negocio adicional documentado | pablo.llorente | 2026-09-23 | RDR_CARGA_BAJA_NIVELES_new / RDR_CONCILIACION_BDI_new |
| Sentido de la dependencia con RDR_BAJAS_CPARTY_new | "RDR_CARGA_BAJA_NIVELES_new es una cadena externa cuyo evento de salida genera un prerequisito operativo de entrada para RDR_BAJAS_CPARTY_new" | pablo.llorente | 2026-09-23 | RDR_CARGA_BAJA_NIVELES_new |
| Dependencias entre jobs — **en `RDR_BBVACONTRACTS_new`** | "En ese caso si" (el sucesor arranca aunque el predecesor acabe KO): de ORDEN, no de éxito. **NO es una regla general de la instalación** — ver la entrada contraria de RDR_EXTRACCION_CONTACTOS | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Fallo de validación XSD — **corregido con export real (2026-09-24)** | Respuesta original en sesión (2026-09-22): "No si no se pasa el XSD da fallo y falla la cadena". **Superada por evidencia de mayor rango:** el export real del Workspace de Control-M muestra `ON NOTOK → DOACTION OK` activo en `VALIDACION_XSD_EXTRACT_BBVA` — el flag "Force OK" del documento fuente original era correcto, la respuesta verbal de esta fila quedó desactualizada. Confirmado por el usuario | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Protocolo ante fallo de extracción | "en caso de fallo se para" | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Aislamiento de envíos | "Los envios no entran en el target de este proyecto pero si fallase un envio por cualquier cosa no afectaria al resto de envios" | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Jobs sin documentar en una ficha | "los que no esten serán porque estan decomisdados" | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Job marcado "(dummy)" | "si aparece como dummy si infroma y ya esta no hay mas info al respecto" | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Entornos de prueba | "Si eson esos entornos pero todavia hay que definirlo entonces continuaremos sin definirlo" | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Viabilidad de datos sintéticos | "si es vaible" (sobre las 19 tablas de KYTL_GC) | pablo.llorente | 2026-09-22 | RDR_BBVACONTRACTS_new |
| Dependencias entre jobs — **en `RDR_EXTRACCION_CONTACTOS`** | "si falla una no puede pasar a la siguiente job" / "si el predecesor falla no puede pasar al posterior": de ÉXITO. **Contradice a RDR_BBVACONTRACTS_new**: el tipo de dependencia es propio de cada cadena y debe preguntarse siempre | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Controles de calidad de la cadena | "Si la extraccion se genera bien se genera bien no hay mas control" — cadena sin filewatcher ni validación XSD, confirmado como deliberado | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Fallo de un hilo en extracción paralela | "Falla la extraccion y se produce un fallo en la cadena" — no hay fichero parcial | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Orden de los registros en salida paralela | "No es relevante" — la validación debe hacerse por conjunto, nunca por diff posicional | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Fichero de salida vacío | "No se puede generar un fichero vacio" — requisito explícito, aunque la cadena no tenga ningún control que lo impida | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Jobs ejecutados como root | "igual es herencia de algo antiguo no tengo el histrorico de por que se hace asi" | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Alcance de SAIT | "Si Sait solo es de Mexico" — confirma el filtro `AgreementORGID='1145' or SCIsBranch='MEX'` de sait.xsl | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Raíz de rutas de trabajo RDR | "La buena es la misma que se usaba en el resto de procesos" → `/fichtemcomp/` (con M). `fichtencomp` es siempre errata de transcripción | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| DataX no es un consumidor | "Hay un problema cuando se dice que otro consumidor es DataX, eso no es asi es que ese archivo se consume por DataX" — DataX es la plataforma; el consumidor real está en el inventario de la wiki | pablo.llorente | 2026-09-22 | RDR_EXTRACCION_CONTACTOS |
| Exclusión `A15` | Confirmada en el SQL literal: `NOT EXISTS` sobre `FT_T_CNTA` con `CONTCT_ASSIGN_STAT_TYP='BRANCH'` y `ORG_ID='A15 '`. **No filtra por `DATA_STAT_TYP`**, a diferencia del bloque `Branches`. **2026-09-24, con query real** (`SELECT ENT_LEG_NME FROM FT_T_ENTR WHERE TRIM(ORG_ID)='A15'`): `A15 = COMPASS`; motivo de negocio confirmado por el usuario — BBVA Compass/BBVA USA fue vendida a PNC en 2020 y ya no forma parte del grupo | pablo.llorente | 2026-09-22 / 2026-09-24 | RDR_EXTRACCION_CONTACTOS |
| Informe SWIFT sin canal de envío (Q4.1) | "El informe Excel SWIFT... no tiene canal de transmisión automatizado por diseño... queda disponible... para consulta local/manual" — confirmado por código fuente (`informeBroker_BDI.wkf`) | pablo.llorente | 2026-09-23 | RDR_CONCILIACION_BDI_new |
| Alerta ante ausencia de fichero de entrada (Q5.1) | "La detención desencadena una notificación de incidencia por correo a ans_rdr.es@bbva.com y la apertura de ticket en Remedy... (código BZG03906)" — a diferencia de otros filewatchers "estrictos" que no especifican este detalle | pablo.llorente | 2026-09-23 | RDR_CONCILIACION_CLIENTELA_new |
| Criticidad de cadena múltiple "W / S / C" (QT1, transversal) — **confirmado, no interpretación**: es un placeholder de cabecera del folder cuyo valor real varía por job: fallo de FileWatcher/historificación ⇒ `W`; abend en motores Java/PL-SQL ⇒ `S`/`C` | pablo.llorente | 2026-09-23 | RDR_CONCILIACION_CLIENTELA_new / RDR_PR_BDICLIENREG_RESP_new / RDR_REFUNDICION_new |
| Asimetría de tolerancia MEKYTL0783/MEKYTL0784 (Q6.1) | "MEKYTL0783 (Estricto)... falla la cadena (NOT OK)" vs. "MEKYTL0784 (Tolerante)... finaliza en OK de forma silenciosa" — mecanismo interno citado (`MEGENV0001.sh`, `FALLA_NO_FICHERO`, `RC=60`) declarado por el usuario sin fichero fuente adjuntado, la conclusión de comportamiento sí se acepta | pablo.llorente | 2026-09-23 | RDR_ENVIO_CLIEX_new |
| `CLIEXCLU.csv` nunca se transmite por XCOM (Q6.2) | "Los sistemas destino (MVP00G219 y MVP00G517) no aceptan el archivo sin procesar/validar. Por ello, el .csv se queda en el servidor local" — solo `CLIEXCLU.txt` (normalizado) se distribuye | pablo.llorente | 2026-09-23 | RDR_ENVIO_CLIEX_new |
| Gestión externa de `controlSCF.txt` (Q7.1) | "La gestión del ciclo de vida del fichero controlSCF.txt... es realizada por un proceso externo a la malla actual, perteneciendo al ámbito de orquestación de... SCF / Investors Plan" | pablo.llorente | 2026-09-23 | RDR_PR_BDICLIENREG_RESP_new |
| Criticidad real de `MEKYTL1300` (G4) | Ficha oficial EX-005-03-MEKYTL1300 exportada de Control-M (23/09/2026): casilla `NIVEL CRITICIDAD` marca únicamente `W`, `S`/`C` sin marcar — corrige el "S / C" del documento fuente original, que queda identificado como errata documental | pablo.llorente | 2026-09-23 | RDR_ExtraccionDUCOMASTERDATA |
| Criticidad real de cadena `RDR_DUCO_CPTY` (G1) | Ficha real "Consulta de Cadena RDR_DUCO_CPTY" en Control-M: campo Criticidad = "Aviso al día siguiente incluso si es festivo" (`S`) — corrige el `F` del documento fuente original, identificado como errata de transcripción | pablo.llorente | 2026-09-24 | RDR_DUCO_CPTY |
| Duplicidad de código ALID entre sucursales | Fichero real de producción `RDR_clientes20250726.csv` (28.689 filas de datos): columna ALID (`numclien`) con 28.689 valores únicos, 0 duplicados encontrados en esta ejecución real | pablo.llorente | 2026-09-24 | Envío a Altamira/Bancomer México |
| Ventana real de `FW_BBVAContracts_RDR_1` | Export real de Control-M: `TIMEFROM="1400" TIMETO="1530"` — compatible con el arranque de la cadena a las 13:00; el 09:30-11:15 del documento fuente era errata/desactualización | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Flag "Force OK" en `VALIDACION_XSD_EXTRACT_BBVA` | Export real de Control-M: `ON NOTOK → DOACTION OK` activo — un XML inválido continúa hacia los 8 destinos; el documento fuente original tenía razón, la respuesta verbal previa del usuario en sesión queda corregida | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Job que genera `BBVAContracts.csv` | `.properties` real de `transformarBBVAContracts` (`MEKYTL0895`): aplica `Agreements_Nodes.xsl` sobre el XML in situ y después `BBVA_Contrats_CSV.xsl` para generar el CSV — es el propio `MEKYTL0895`, no un job independiente | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Generación de ficheros de Mentor en `RDR_BBVACONTRACTS_new` | "Para contratos Mentor está fuera, aplícalo" — decomisión reconfirmada específicamente para esta cadena, con independencia de que el `.properties` de entorno "ei" conserve los pasos de generación. **No extensible a otros procesos**: `MEKYTL1266` (Calendarios a Modelity) y `MEKYTL1146` (`RDR_ISSUES_RE_PRO_new`) siguen activos y envían a Mentor | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Estado real de `MEKYTL1053` | Ficha EX-005-03 real: `MÁQUINA ORIGEN 22.156.148.85` (IP fija antigua, no la VIPA `pr-rdr.igrupobbva` vigente) y "Reglas Planificación/Periodicidad" en blanco — a diferencia de todas las demás fichas activas de la cadena. Consistente con su ausencia del export real de Control-M: probablemente decomisado o nunca migrado | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Identidad real de `MEKYTL1246`/`MEKYTL1264`/`MEKYTL1307` | 3 ramas de distribución reales y activas (fichas EX-005-03 + export de Control-M), no decomisadas como se asumía: `MEKYTL1246`→GMIP, `MEKYTL1264`→NOVA THOR, `MEKYTL1307`→PXVA, todas vía `novatransferbatch.igrupobbva`, predecesor `MEKYTL0895`, sucesor `MEKYTL0953` | pablo.llorente | 2026-09-24 | RDR_BBVACONTRACTS_new |
| Topología real del árbol de jobs de Calendarios a Modelity | Export XML real de Control-M (`INCOND`/`OUTCOND` de cada job): cadena estrictamente secuencial, sin Fan-Out/Fan-In — `MEKYTL1090` depende de `MEKYTL1113`, no del filewatcher; `MEKYTL1184`/`MEKYTL1184_DUMMY` y `MEKYTL1311`/`MEKYTL1311_DUMMY` son pares de alternancia por calendario (mismo predecesor y mismo evento de salida). Un fallo en cualquier job bloquea todos los posteriores | pablo.llorente | 2026-09-24 | Envío de Calendarios a Modelity |
| Mecanismo de limpieza en pasarela `lpftp503` de `RDR_EXTRACCION_CONTACTOS` | Fichas reales EX-005-03-MEKYTL1189/MEKYTL1189_SND: origen y destino intermedio en la pasarela usan el mismo nombre fijo `RDR_contactosSAIT.xml` (sin fecha); el nombre con fecha solo se aplica en el segundo salto (pasarela→SAIT). Confirma la sobrescritura diaria sin necesidad de job de borrado | pablo.llorente | 2026-09-24 | RDR_EXTRACCION_CONTACTOS |
| `ArgJava3=20` en `ExtraccionGenericaCONT.properties` | Confirmado por completo: `.properties` real da el valor (posición 3 de argumentos Java), y el código fuente real de `Ppal.java` confirma `int NUM_THREADS = Integer.parseInt(args[2])`, usado en `Executors.newFixedThreadPool(NUM_THREADS)` — ya no es interpretación, es un hecho de código | pablo.llorente | 2026-09-24 | RDR_EXTRACCION_CONTACTOS |

## 3. Lecciones de estructuración
- El documento de análisis "funcional" de un proceso RDR suele describir el diccionario de datos de forma simplificada/idealizada; el diccionario real del fichero físico puede diferir sustancialmente (nombres de campo, número de campos, semántica de dominio). Verificar siempre antes de construir la especificación técnica. _(pablo.llorente, 2026-09-17)_
- Pregunta clave para detectar gaps: "¿el campo X que mencionas en la clave de negocio realmente viaja en el fichero físico, o es solo un campo interno de la base de datos de origen?" — permitió destapar que MARKET_CODE/CAL_ID no existen en el CSV real. _(pablo.llorente, 2026-09-17)_
- Pregunta clave para detectar gaps: "¿el fichero contiene una fila por cada día, o solo las excepciones (no-hábiles)?" — crítica para no asumir la semántica de un campo tipo flag/enum. _(pablo.llorente, 2026-09-17)_
- Regla documentada: cuando un job de distribución no valida contenido (solo presencia/timing), no asumir que hay control de calidad aguas abajo dentro de la misma cadena — documentarlo como riesgo/gap de diseño explícito, no como comportamiento validado. _(pablo.llorente, 2026-09-17)_
- Regla documentada: cuando una respuesta del usuario cita como evidencia un documento o fichero por nombre/ruta, **verificar directamente contra el repositorio real** (working tree + todas las ramas remotas) antes de aceptarlo como evidencia, en vez de asumir que existe solo porque se cita con detalle. _(pablo.llorente, 2026-09-17, Envío a Altamira Colombia)_
- Patrón de falacia a vigilar: **usar un dato técnico de ejecución (usuario "Run As" de un job en Control-M) como si fuera prueba de una estructura organizativa de soporte/escalado.** Un "Run As" es la cuenta de sistema operativo bajo la que corre el proceso, no implica pertenencia a un equipo humano con SLA. _(pablo.llorente, 2026-09-17, Envío a Altamira Colombia)_
- Patrón de riesgo a vigilar en cadenas con bifurcación paralela: **verificar siempre si TODAS las ramas paralelas están conectadas al marcador/evento de cierre de la cadena, no solo asumirlo.** En Altamira/Bancomer México se detectó que solo la rama de historificación cerraba la cadena; la rama de transmisión a un sistema externo (DataX) tenía su propio evento de éxito pero no bloqueaba ni se reflejaba en el cierre — Control-M podía marcar la cadena como completada aunque esa rama fallara. _(pablo.llorente, 2026-09-18, Envío a Altamira/Bancomer México)_
- Cuando un gap de negocio (p. ej. el motivo de una exclusión hardcodeada en una query) no se puede resolver ni con documentación ni con los metadatos de las tablas (columnas de estado como `data_stat_typ` no lo explican), cerrarlo como en el caso del rango de `Calendarios.csv`: confirmar el comportamiento con evidencia (bytecode, datos reales), pero dejar el motivo de negocio como gap abierto sin inventarlo. _(pablo.llorente, 2026-09-18, Envío a Altamira/Bancomer México)_
- Cuando un sistema documenta varias cadenas Control-M como un único "sistema"/proceso de negocio (p. ej. P-021 con 8 cadenas), preguntar explícitamente al usuario si prefiere una especificación por cadena o una única especificación consolidada, en vez de asumir la granularidad — el tamaño y la complejidad de tratar 8 cadenas en un solo intake sin acotar el alcance puede ser inmanejable. _(pablo.llorente, 2026-09-21, Sistema P-021)_
- Cuando faltan la query SQL y el diccionario de datos de un fichero generado (y no hay muestra real disponible), no forzar un caso de prueba de tipo `datos_sinteticos` inventando una estructura — documentar la limitación de evidencia explícitamente y, si aplica, reinterpretar `duplicidad` a nivel de re-ejecución/fichero completo en lugar de a nivel de registro individual. _(pablo.llorente, 2026-09-21, RDR_BANCARIZACION_new)_
- Patrón transversal confirmado para todo el sistema P-021 (aplicable por defecto sin volver a preguntarlo cadena por cadena, salvo evidencia explícita en contrario): sin validación de integridad de negocio ni protección de concurrencia/lock documentadas. Confirmado explícitamente por el usuario para Calendarios, Altamira Colombia, Altamira México, `RDR_BANCARIZACION_new` y, por extensión, el resto de cadenas de P-021 aún no analizadas. La ausencia de documentación es el criterio de clasificación, no una prueba de ausencia técnica real en runtime. _(pablo.llorente, 2026-09-23, RDR_CARGA_BAJA_NIVELES_new)_
- Cuando una cadena tiene una dependencia con otra cadena externa no documentada en el material, distinguir el sentido de la relación antes de descartarla como "fuera de alcance": si esta cadena **alimenta** (es prerrequisito operativo de) la externa, documentar esa relación saliente explícitamente en vez de simplemente omitirla; solo la implementación interna de la cadena externa queda fuera de alcance. _(pablo.llorente, 2026-09-23, RDR_CARGA_BAJA_NIVELES_new)_
- Patrón de riesgo a vigilar en cadenas con Fan-Out sin Fan-In: cuando varias ramas paralelas terminan de forma independiente sin converger en un marcador de cierre único, no asumir que existe un indicador global de "cadena completada" — cada rama debe verificarse por separado. _(pablo.llorente, 2026-09-21, RDR_BANCARIZACION_new)_
- No todas las cadenas con Fan-Out tienen el riesgo anterior: verificar caso por caso. `RDR_ISSUES_RE_PRO_new` sí implementa un Fan-In correcto (convergencia AND explícita en `MEKYTL0536`, `MEKYTL1139`, `MEKYTL0981` y `MEKYTL1028`) — no asumir el patrón de riesgo por generalización de procesos anteriores, hay que confirmarlo release/proceso. _(pablo.llorente, 2026-09-22, RDR_ISSUES_RE_PRO_new)_
- **Distinguir siempre, en la especificación, entre "confirmado por documento fuente verificable" y "declarado por el usuario en sesión sin fichero adjunto"**: la regla del agente permite cerrar gaps con la declaración del usuario en sesión (sin exigir fichero), pero eso no lo convierte en "evidencia documental auditada" — mantener la distinción explícita en la sección de riesgos de `spec.md`, especialmente cuando el usuario insiste varias rondas en un mismo dato sin aportar el fichero solicitado. _(pablo.llorente, 2026-09-22, Carga multipetición Refinitiv emisores)_
- Reforzado el patrón de verificación activa de evidencia citada: una cita de rutas de fichero como prueba ("carpeta del repositorio", `C:\...`) debe verificarse con `git fetch --all` + búsqueda en todas las ramas antes de aceptarla — en esta sesión, tras verificarse, las rutas citadas no existían en ninguna rama (mismo patrón que DOC-ALT-002, ya documentado). No aceptar la calificación de "verificado por artefactos reales del repositorio" que el propio usuario proponía sin comprobarlo primero. _(pablo.llorente, 2026-09-22, RDR_ISSUES_RE_PRO_new)_
- El mismo patrón de citar código fuente detallado (rutas `C:\RDR\kdd-nfq-rdr-project-206\...`, líneas exactas, snippets) sin adjuntar el fichero real se repitió una tercera vez (procesos DUCO). Al pedir explícitamente el fichero/log real, el usuario sí lo adjuntó, y la lectura directa confirmó el comportamiento afirmado (publicación incondicional de fichero vacío en 2 jars distintos) — la vía correcta ante estas citas es pedir siempre el fichero real antes de aceptar el hallazgo como cerrado, nunca conformarse con el nivel de detalle narrado. _(pablo.llorente, 2026-09-23, RDR_DUCO_CPTY / RDR_ExtraccionDUCOMASTERDATA)_
- Patrón de comportamiento confirmado por código fuente en generadores de extracción genérica RDR: ninguno de los jars analizados (`ExtraccionGenericaOtherEntities`/`Ppal.java`, `ExtraccionGenericaUnificada`/`Principal.java`) tiene gate de conteo mínimo — publican el fichero de salida incondicionalmente aunque la consulta devuelva 0 filas. Puede ser un patrón transversal a más procesos que usen el mismo tipo de motor; verificar caso por caso, no asumir sin evidencia para procesos aún no analizados. _(pablo.llorente, 2026-09-23, RDR_DUCO_CPTY / RDR_ExtraccionDUCOMASTERDATA)_
- Cuando el usuario adjunta una imagen embebida como referencia Markdown (`![][imageN]` con un `[imageN]: <data:image/...;base64,...>` al final del fichero), el Read tool no la renderiza automáticamente si el bloque base64 excede el límite de tokens — hay que extraer el payload base64 con `awk`/`sed`, decodificarlo a fichero (`base64 -d`) y releerlo como imagen aparte. No dar por perdida la evidencia solo porque aparezca truncada como texto. _(pablo.llorente, 2026-09-23, RDR_CLIENTES_CIB_new)_
- Nuevo patrón de comportamiento de filewatcher confirmado por captura real de Control-M (no solo texto): ante timeout (RC=7), un job puede quedar marcado como **OK** y aun así emitir un evento de salida distinto al de éxito (sufijo `_KO` en vez de `_OK_new`) — un tercer patrón, distinto tanto de la alerta por email como del soft-failure silencioso ya vistos. Verificar siempre los 2 posibles códigos de retorno (0 y 7) y sus acciones asociadas por separado, nunca asumir que ambos siguen el mismo patrón que otro filewatcher ya analizado. _(pablo.llorente, 2026-09-23, RDR_CLIENTES_CIB_new)_
- **Fichero huérfano:** buscar siempre ficheros que algún job lee pero que ningún job documentado genera. Ha aparecido en dos procesos: `DictionaryIndex_TOTAL.csv` (lo generaba el Planificador Genérico, externo a la cadena) y `BBVAContracts.csv` (lo genera el transformador `BBVA_Contrats_CSV.xsl` declarado en el properties). _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- Cuando el usuario cuestiona un dato del análisis ("¿de dónde sacas eso?"), tener localizada la **cita literal del documento fuente** resuelve la discusión en un paso y suele revelar que el error está en la ficha, no en el análisis. _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- Las fichas de Control-M contienen datos obsoletos con frecuencia: ventanas de filewatcher anteriores a la hora de arranque de la cadena, flags "Force OK" que no reflejan el comportamiento real, nombres de job renombrados sin actualizar el documento de la cadena. Contrastar siempre contra el usuario y dejar la verificación en Control-M como riesgo explícito. _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- Cuando el usuario responde a una tanda de preguntas **devolviendo a su vez preguntas propias**, hay que responderlas y cerrar el ciclo antes de generar la salida — no generar directamente asumiendo que la tanda está cerrada. _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- Decomisiones en cascada: al confirmarse que un destino está decomisado, revisar qué jobs del flujo vivo tenían ese destino como única función documentada — pueden seguir siendo nodos de secuencia imprescindibles para el resto de ramas (caso `MEKYTL0895`, cuyo nombre y descripción aluden a una transformación a Mentor que ya no realiza). _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- Cadencias mixtas dentro de una misma rama: un job de envío mensual encadenado con jobs de historificación diaria solo es coherente si las dependencias son de orden y no de éxito. Detectar el patrón "predecesor con calendario restringido" y preguntar explícitamente por él, porque de lo contrario parece un bloqueo permanente. _(pablo.llorente, 2026-09-22, RDR_BBVACONTRACTS_new)_
- **Pedir siempre el `.properties` y las hojas XSLT/XSL, no solo el documento de análisis.** En `RDR_EXTRACCION_CONTACTOS` la lógica de negocio más importante del proceso —que SAIT recibe solo contactos de México— no estaba en la ficha de ningún job: vivía en `sait.xsl`, porque la transformación no es un job de la cadena sino una acción interna del job de extracción. Un análisis basado solo en las fichas de Control-M la habría pasado por alto por completo. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- El tipo de dependencia entre jobs de Control-M (orden frente a éxito) **es propio de cada cadena y no una convención de la instalación**: `RDR_BBVACONTRACTS_new` y `RDR_EXTRACCION_CONTACTOS` están configuradas de forma opuesta. Preguntarlo siempre, nunca deducirlo de otro proceso. _(pablo.llorente, 2026-09-22)_
- Cuando el usuario confirma un requisito de negocio ("no se puede generar un fichero vacío") pero el código analizado no tiene ningún control que lo haga cumplir, **eso no es un gap de documentación sino un hallazgo**: documentarlo como discrepancia entre requisito e implementación, con riesgo propio y un caso de prueba que se espera que falle. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Un código o filtro hardcodeado que aparece en un proceso conviene buscarlo en la memoria compartida antes de preguntarlo: `1145` ya estaba registrado por otro compañero desde el análisis de Altamira/Bancomer México, lo que permitió identificarlo como la sucursal de México sin necesidad de consultar. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Extracción paralela por hilos: preguntar siempre dos cosas que el documento nunca trae — qué pasa si falla un hilo (¿fichero parcial silencioso?) y si el orden de los registros de salida es relevante (condiciona si se puede validar por diff). _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- **Pedir el SQL literal aunque el documento traiga un diccionario de datos aparentemente completo.** En `RDR_EXTRACCION_CONTACTOS` el SQL aportado en sesión corrigió la estructura del fichero (hay un bloque `Contacts` por contacto, no uno con muchos `ContactDetail`), cerró un gap que el usuario no sabía responder, confirmó un código que se había llegado a borrar de la spec por no poder verificarlo, y destapó un defecto —`rownum=1` sin `ORDER BY`— que ninguna descripción en prosa habría revelado. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Cuando una contradicción entre dos fuentes parece irresoluble, considerar que ambas puedan ser descripciones parciales del mismo hecho antes que suponer que una se equivoca: `StarDate` era a la vez un dato del contacto (como decía el documento) y un hermano de `ContactDetail` (como evidenciaba la XSLT), porque cada contacto genera su propio bloque. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- **"Envío a DataX" en una ficha de Control-M nunca significa que DataX sea el consumidor.** DataX es la plataforma de transferencia: el job solo copia el fichero a `/unload/kytl/datsal/datax` y el sistema destino monta la transferencia por su cuenta, pudiendo cambiar nombre, hora y ruta sin avisar a RDR. Antes de nombrar un destino, consultar el inventario en `memoria/memoria_datax_RDR.md`. Las fichas inducen al error: la de `MEKYTL1177` etiquetaba el correo de IHS Markit como "Contacto Aplicativo Destino (DataX)". _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Cuando un job deposita un fichero en un directorio compartido para que otro sistema lo recoja, el criterio de aceptación del caso de prueba termina en ese directorio: lo que ocurre después no es observable desde la cadena y no debe prometerse en la spec. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Revisar los `rownum` y los `TOP/LIMIT` sin `ORDER BY` en cualquier SQL analizado: producen resultados no deterministas y silenciosos. Si además la columna afectada alimenta un filtro de negocio aguas abajo, el defecto deja de ser una pérdida de datos y pasa a ser un incumplimiento de la regla de negocio. _(pablo.llorente, 2026-09-22, RDR_EXTRACCION_CONTACTOS)_
- Cuando una cadena de un mismo sistema (P-021) dispara externamente el arranque de otra cadena del propio sistema, y **ambas secciones del documento fuente se citan mutuamente** (la emisora documenta el evento de salida hacia la receptora, y la receptora documenta el mismo evento como su prerrequisito de entrada), la dependencia queda **auto-confirmada por referencia cruzada dentro del propio documento** — no hace falta preguntarlo al usuario. Caso: `RDR_REFUNDICION_new` (`KYTL_REF_GSPROCESS`) → `RDR_CONCILIACION_CLIENTELA_new` (`KYTL_CONCLI_GSPROCESS_FW`, Ext.). _(pablo.llorente, 2026-09-23, RDR_REFUNDICION_new / RDR_CONCILIACION_CLIENTELA_new)_
- Un gap transversal ya resuelto para una cadena de un sistema multi-cadena (p. ej. el placeholder de criticidad "W / S / C") se reutiliza directamente para las demás cadenas del mismo sistema que presenten el mismo patrón, sin volver a preguntarlo — confirmado explícitamente por el usuario que aplica a las 3 cadenas de P-021 con esa combinación de letras (`RDR_CONCILIACION_CLIENTELA_new`, `RDR_PR_BDICLIENREG_RESP_new`, `RDR_REFUNDICION_new`). _(pablo.llorente, 2026-09-23, Sistema P-021)_
- Con `RDR_REFUNDICION_new` se completan las 8 cadenas del sistema P-021 (48 pasos totales). Patrón de cierre de sistema multi-cadena: cada cadena se especifica de forma independiente en `salidas/`, pero las dependencias cruzadas (entrada y salida) se documentan explícitamente en ambos lados, nunca solo en el que las menciona primero. _(pablo.llorente, 2026-09-23, Sistema P-021)_
- Una inconsistencia documental (p. ej. criticidad dual "S / C" a nivel de job frente a "W" a nivel de cadena) que no se puede resolver por inferencia sí puede resolverse más adelante si el usuario aporta la **ficha oficial vigente exportada directamente de Control-M** (no el documento de análisis original) — no tratarla como cerrada para siempre solo porque en su momento se aceptó como "inconsistencia sin resolver"; si llega evidencia nueva, reabrir y corregir el gap. En este caso la ficha real mostró `W` únicamente, y el "S / C" del documento fuente quedó identificado como errata de ese documento, no como el valor operativo real. _(pablo.llorente, 2026-09-23, RDR_ExtraccionDUCOMASTERDATA)_
- Cuando un riesgo abierto no se puede verificar (p. ej. el comportamiento del sistema destino solo es observable en un viernes festivo, y el último ocurrido no tiene log accesible) y el usuario no quiere que quede etiquetado como "no verificado", la alternativa correcta no es inventar una confirmación: es **retirar la etiqueta de riesgo/pendiente y sustituirla por una nota de procedimiento** que describa cómo se verificaría en el futuro (qué evidencia buscar y cuándo estará disponible), dejando claro que la decisión de diseño en sí (la parte que el usuario sí confirmó) permanece cerrada y separada de la parte no verificable. _(pablo.llorente, 2026-09-24, Envío de Calendarios a Modelity)_
- Para cerrar un gap sobre "¿este límite es fijo o dinámico?" con evidencia real (no una interpretación de una única fuente), cruzar **dos artefactos independientes de origen distinto** (una consulta en vivo contra la tabla origen + un fichero de producción real ya exportado) buscando patrones de correlación exacta (mismo mes/día, desfase constante en años) es más concluyente que aceptar cualquiera de los dos por separado. En Calendarios a Modelity esto reveló un hallazgo no solicitado (un recorte deliberado de 6 años en la extracción, para proteger contra ambigüedad de formato de fecha en sistemas destino) que ninguna de las dos fuentes por sí sola habría revelado. _(pablo.llorente, 2026-09-24, Envío de Calendarios a Modelity)_
- **No confiar en la lectura visual de un diagrama de flujo de Control-M comprimido/pequeño para determinar dependencias reales (Fan-Out/Fan-In vs. cadena secuencial).** En Calendarios a Modelity, una primera lectura del gráfico llevó a inferir una topología de Fan-Out/Fan-In que resultó ser incorrecta (el propio usuario corrigió un cruce de flechas). La fuente fiable es el **export XML del Workspace de Control-M** (bloques `INCOND`/`OUTCOND` por job), que resolvió la topología real sin ambigüedad: cadena estrictamente secuencial con 2 pares de jobs en alternancia por calendario (mismo predecesor y mismo evento de salida), no un Fan-Out/Fan-In real. Cuando la topología de un grafo es crítica para el riesgo (bloqueo en cascada), pedir directamente este export en vez de conformarse con una captura del diagrama visual. _(pablo.llorente, 2026-09-24, Envío de Calendarios a Modelity)_
- Un export real de Control-M puede revelar comportamiento no documentado en el material original y corregir supuestos ya escritos en la especificación (no solo cerrar gaps nuevos): en este caso reveló que el filewatcher tiene un `DOACTION RERUN` explícito ante timeout, contradiciendo la asunción previa de "KO directo por timeout" — y abrió un gap nuevo (límite de reintentos) que no existía antes de tener esta evidencia. _(pablo.llorente, 2026-09-24, Envío de Calendarios a Modelity)_
- Otra instancia del patrón "errata de transcripción del documento fuente, no discrepancia real" (ya visto en las criticidades de `MEKYTL1300` y de la cadena `RDR_DUCO_CPTY`): la ficha real EX-005-03-MEKYTL0157 mostró que el fichero origen y el destino usan la misma cadena continua "ListadoClientesBancarizacion" sin ningún espacio en ninguno de los dos nombres — el espacio que aparecía en el documento fuente original entre "Clientes" y "Bancarizacion" era una errata de quien transcribió el análisis, no un requisito real del receptor MVP00G215. La misma ficha, sin haberse preguntado por ello, también corrigió la criticidad documentada del job de W a C (aviso inmediato) — cuando llega una ficha real para cerrar un gap concreto, revisar el resto de sus campos por si contradicen otro dato ya asumido en la spec (aquí, la criticidad "en los 3 envíos" de R6). _(pablo.llorente, 2026-09-24, RDR_BANCARIZACION_new)_
- **Cuando un motor de reporte (`RDR_Report.jar`/clase `CreateReport`) no lleva el SQL hardcodeado, buscar en el propio documento fuente si otro proceso que use el mismo motor ya documenta dónde vive la query** (aquí, ConBDI y Refundición documentaban explícitamente que la consulta está en un `select.properties` compartido, bajo una clave por módulo) — permite pedir exactamente el fichero correcto en vez de aceptar "no disponible" como definitivo. Una vez obtenido, la query real resolvió de un guante 9 campos exactos contra el esquema GoldenSource (`FT_T_CUST`/`FT_T_FRID`/`FT_T_FIID`/`FT_T_FIST`/`FT_T_FIGU`), y cruzarla con una muestra real de producción (98.905 registros) permitió confirmar además que 2 de esas 9 columnas (`ORIGBANC`/`ORIGOFIC`) están definidas en el esquema pero vacías al 100% en producción — un hallazgo que ni la query ni la muestra por separado habrían revelado con la misma certeza. _(pablo.llorente, 2026-09-24, RDR_BANCARIZACION_new)_
- Antes de marcar como "duplicidad de datos" una coincidencia de valor entre varias filas de un fichero aplanado, verificar si el modelo de datos permite legítimamente esa cardinalidad 1-a-N: en la muestra real de bancarización, decenas de entidades (p. ej. `BANCO DE SABADELL S.A.`) tienen varias filas con el mismo identificador fiscal (col.5) pero distinto identificador de relación STAR/MUREX (col.2, una fila por sucursal/relación registrada) — es el comportamiento esperado del modelo, no un defecto de duplicidad como el de ALID en Altamira/Bancomer México. _(pablo.llorente, 2026-09-24, RDR_BANCARIZACION_new)_
- **Un workflow GoldenSource real (`.wkf`, XML de `com.j2fe.workflow.definition.Workflow`) puede resolver solo una parte de un gap de "detalle interno no documentado" y aun así valer la pena analizarlo a fondo.** El workflow `Refinitiv_Request_Response.wkf` confirmó con código real la mecánica de los 2 primeros jobs de la cadena (switch por `requestType`/`vreqOid`, tablas que toca directamente: `FT_T_VREQ`, `TABLEALERTGENER`), pero las tablas de ratings declaradas (`FT_T_FIRT`/`FT_T_RLT1`) no aparecían en ningún `UPDATE`/`INSERT` del fichero — un `grep` ingenuo de esos nombres de tabla habría concluido erróneamente que el workflow "no las toca". Localizar los nodos `CallSubWorkflow` y extraer el nombre del sub-workflow invocado (`Refinitiv_Load_Ratings`, en este caso) reveló que la responsabilidad está delegada, dando una pista concreta y nueva para seguir pidiendo evidencia (el fichero de ese sub-workflow) en lugar de cerrar el gap en falso o declararlo irresoluble. _(pablo.llorente, 2026-09-24, RDR_BATCH_EMISORES_REFINITIV)_
- **Cuando un job de copia y su job sucesor de reenvío usan nombres de fichero distintos en cada salto, comparar los nombres literales de origen y destino de cada ficha basta para determinar si hay sobrescritura, sin necesitar logs.** En `RDR_EXTRACCION_CONTACTOS`, `MEKYTL1189` deposita siempre `RDR_contactosSAIT.xml` (nombre fijo) en la pasarela, y es el job siguiente (`MEKYTL1189_SND`, ya en la propia pasarela) el que añade la fecha al reenviar al destino final — el hecho de que el nombre en la pasarela nunca cambie ya demuestra la sobrescritura diaria, sin necesitar un log de dos ejecuciones consecutivas. _(pablo.llorente, 2026-09-24, RDR_EXTRACCION_CONTACTOS)_
- **Un valor de configuración idéntico entre 2 `.properties` de cadenas distintas no implica colisión funcional si el mecanismo real que lo consume no lo usa para decidir el destino.** `id=MULTI` aparece literalmente en `RDR_Refinitiv_REQ_RES.properties` (`idType=ORG_ID`) y en `RefinitivIssueMultiRequest.properties` (`idType=MULTI`) — confirmado real con los 2 ficheros, no una errata de transcripción. Pero el workflow real (`Refinitiv_Request_Response.wkf`, ya analizado) solo usa `id` como texto literal dentro del nombre del fichero de salida; el directorio de destino depende de `requestType`+`vreqOid`, que difieren entre ambos casos. Antes de calificar una coincidencia de valor como "inconsistencia" o "posible bug", localizar el código real que consume esa variable y comprobar si realmente determina un comportamiento compartido o solo aparenta hacerlo por coincidencia de nombre. _(pablo.llorente, 2026-09-24, RDR_BATCH_EMISORES_REFINITIV)_
- **Al pedir metadatos de auditoría (`LAST_CHG_USR_ID`/`LAST_CHG_TMS`/`DATA_STAT_TYP`) de una tabla de configuración GoldenSource para resolver una pregunta concreta, revisar todos los campos devueltos, no solo el que se pidió.** Al consultar `FT_T_ATE1` para saber quién tocó por última vez la query maestra de `RDR_EXTRACCION_CONTACTOS`, apareció que su propio `DATA_STAT_TYP` es `INACTIVE` — un hallazgo no buscado y potencialmente mucho más grave que la pregunta original (¿sigue funcionando la extracción con la query "marcada como inactiva"?). Se resolvió de forma empírica (el usuario confirmó que el fichero de salida real seguía siendo reciente y con contenido), sin necesitar el código exacto del motor — cuando la evidencia de código no está disponible, un hecho observable reciente (fecha de modificación + contenido no vacío de la salida real) puede bastar para descartar un riesgo funcional activo, aunque no explique el mecanismo. _(pablo.llorente, 2026-09-24, RDR_EXTRACCION_CONTACTOS)_
- **Cuando varios procesos independientes declaran el mismo tipo de comportamiento ("estricto" vs. "Soft Failure") a través del mismo motor genérico (`MEGENV0001.sh`), conseguir el fichero fuente de ese motor UNA VEZ cierra el mecanismo para todos los procesos, no solo para el que lo pidió.** El script real confirmó de un solo vistazo la variable `FALLA_NO_FICHERO` y sus códigos de retorno exactos, validando retroactivamente asimetrías ya aceptadas por declaración de usuario en `RDR_CONCILIACION_CLIENTELA_new`, `RDR_REFUNDICION_new` y `RDR_PR_BDICLIENREG_RESP_new`, además de resolver el gap explícito de `RDR_ENVIO_CLIEX_new`. Antes de pedir el fichero fuente de un motor genérico ya visto en varios procesos, revisar si ya se declaró en alguno de ellos para no perder de vista que la evidencia sirve a todos a la vez. _(pablo.llorente, 2026-09-24, RDR_ENVIO_CLIEX_new)_
- **Un script de validación real puede confirmar el algoritmo declarado y aun así revelar un defecto que nadie había preguntado.** `RDR_Validacion_XSD.sh` confirmó al detalle el troceado (1.000 registros) y el paralelismo (20 procesos) ya declarados por el usuario, pero la lectura completa del script reveló que solo el chequeo de balance de etiquetas puede hacer fallar el job — la validación real contra el XSD (`xmllint --schema`) nunca propaga un `exit` distinto de 0, por diseño de la función `validacion()`. Cuando llega un script/fichero fuente real para cerrar un gap ya "aceptado" por declaración del usuario, revisar el control de flujo completo (no solo la parte que responde a la pregunta original) antes de darlo por cerrado sin más. _(pablo.llorente, 2026-09-24, RDR_ISSUES_RE_PRO_new)_
- **Cuando un gap de "detalle interno no documentado" involucra una cadena de 3 jobs con delegación en cascada, seguir la cadena de `CallSubWorkflow` hasta el final antes de cerrar el gap.** `Refinitiv_Request_Response` delegaba en `Refinitiv_Load_Ratings`, que a su vez invocaba un jar externo (`Refinitv_Ratings.jar`) y dejaba la responsabilidad última sobre `FT_T_FIRT`/`FT_T_RTNG`/`FT_T_RVXR`/`FT_T_RTVL` al tercer workflow de la cadena (`BBG_Refinitiv_Batch`), que resultó tener el `UPDATE FT_T_FIRT` explícito y el mapeo real entre 5 agencias de rating (S&P/Moody's/Fitch/DBRS/Scope). Pedir los 3 ficheros de una vez (en lugar de conformarse con el primero) permitió cerrar el gap G1 por completo y descubrir 2 tablas nuevas (`FT_T_FRRL`/`FT_T_INCL`) que ni el documento fuente ni la declaración del usuario habían mencionado. _(pablo.llorente, 2026-09-24, RDR_BATCH_EMISORES_REFINITIV)_
- **Heurística para detectar una ficha EX-005-03 de un job decomisado o nunca migrado, sin necesitar una confirmación explícita:** (a) `MÁQUINA ORIGEN`/`MÁQUINA DE EJECUCIÓN` con una IP fija antigua (p. ej. `22.156.148.85`) en vez de la VIPA vigente que usan el resto de jobs activos de la misma cadena, y (b) el campo "Reglas Planificación/Periodicidad" completamente en blanco, cuando el resto de fichas de la cadena sí lo declaran. La combinación de ambas señales en `MEKYTL1053` (`RDR_BBVACONTRACTS_new`) fue consistente con su ausencia total del export real de Control-M. Aplicar esta heurística en futuros procesos antes de pedir una confirmación explícita cuando ya se disponga de la ficha del job en cuestión. _(pablo.llorente, 2026-09-24, RDR_BBVACONTRACTS_new)_
- **Una afirmación general del usuario ("todo X está decomisado") debe contrastarse contra lo ya confirmado en otros procesos antes de aplicarla sin más.** Cuando el usuario dijo "todo lo de generación de ficheros de Mentor está decomisado, tenlo en cuenta para próximos y para los que ya tienes", esa frase —tomada literalmente— habría contradicho 2 hechos ya confirmados con evidencia real en esta misma sesión (`MEKYTL1266` en Calendarios a Modelity y `MEKYTL1146` en `RDR_ISSUES_RE_PRO_new`, ambos activos y enviando a Mentor). Antes de aplicar la instrucción, se preguntó explícitamente si el alcance era la cadena concreta en discusión (`RDR_BBVACONTRACTS_new`) o todo RDR; el usuario confirmó que era solo la primera. Nunca generalizar una instrucción amplia sin comprobar que no pisa hechos ya cerrados en otros procesos del mismo lote de memoria. _(pablo.llorente, 2026-09-24, RDR_BBVACONTRACTS_new)_
- **"Los pasos no documentados están decomisados" es una hipótesis, no una conclusión, hasta que se contrasta con un inventario real.** En `RDR_BBVACONTRACTS_new` se había aceptado verbalmente que los ~8 pasos no identificados de los 30 declarados eran todos jobs decomisados de la rama Mentor. El export real de Control-M reveló que 3 de ellos (`MEKYTL1246`, `MEKYTL1264`, `MEKYTL1307`) eran en realidad ramas de distribución activas hacia 3 destinos completamente nuevos (GMIP, NOVA THOR, PXVA) — confirmados además por sus propias fichas EX-005-03. La hipótesis de decomisión solo era cierta para la rama Mentor (3 jobs), no para el resto. Cuando "N pasos sin identificar" se explica íntegramente por una única causa (decomisión), verificar con el inventario real en vez de dar la explicación por buena para todos los casos restantes. _(pablo.llorente, 2026-09-24, RDR_BBVACONTRACTS_new)_
- **Una respuesta verbal del usuario en sesión, sin fichero fuente, puede quedar corregida por un export real de Control-M aportado después** — incluso cuando esa respuesta llevaba varias rondas dándose por buena y ya estaba en la especificación como "confirmado por el usuario". En `RDR_BBVACONTRACTS_new`, la respuesta previa ("el fallo de XSD detiene la cadena") resultó ser la incorrecta: el export real mostró `ON NOTOK → DOACTION OK` activo, coincidiendo con lo que decía el documento fuente original que se había descartado. Al presentar la contradicción, no se sobrescribe silenciosamente la respuesta anterior: se expone la evidencia y se pide confirmación explícita antes de invertir la conclusión (en este caso, el usuario confirmó "es correcto"). El mismo export resolvió además, sin necesidad de preguntar de nuevo, la ventana horaria real del filewatcher (14:00-15:30), que sí coincidía con lo ya asumido. _(pablo.llorente, 2026-09-24, RDR_BBVACONTRACTS_new)_

## 4. Registro de procesos ya analizados
| Proceso | Usuario | Fecha | Documento de salida generado |
|---------|---------|-------|-------------------------------|
| RDR_EXTRACCIONSSIS (Extracción genérica de SSIs) | pablo.llorente | 2026-09-16 | `salidas/spec_RDR_EXTRACCIONSSIS_SIMULACRO.md` — SIMULACRO con 13 supuestos sin confirmar (eliminado en V1.8 de la rama) |
| RDR_DICTIONARY_INDEX_new + RDR_FIC_DAT_DICT_WEEKLY_SEND_new | pablo.llorente | 2026-09-17 | `salidas/rdr_dictionary_index_y_weekly/` → `spec.md` + `prerrequisitos.md` + `casos_prueba.xml` (13 TC); gap abierto: protocolo fallo RDRKYTL001 pendiente ANS RDR; cadena semanal DORMIDA (Planificador INACTIVO) |
| Envío de Calendarios a Modelity (ENVIO_CAL_MODELITY_new) | pablo.llorente | 2026-09-17 | salidas/envio_calendarios_modelity/ — **2026-09-24:** límite de reintentos del filewatcher cerrado con ficha real EX-005-03-KYTL_CAL_MODELITY_FW: no es un contador, es la ventana horaria (22:00-23:00) coincidente con el timeout interno del `ctmfw` (60 min). Sin pendientes bloqueantes |
| Envío a Altamira Colombia (P-035 / RDR_ALTAMIRA_COLOMBIA_SEND) | pablo.llorente | 2026-09-17 | salidas/envio_altamira_colombia/ — **2026-09-24:** manejo de duplicados dentro de `RDR_ConciliaColombia.jar` cerrado con código fuente real (`ColombiaEnvio.java`/`Querys.java`): `obtenerIDs()` usa `SELECT DISTINCT`, el fichero generado nunca puede contener duplicados. Sin pendientes bloqueantes |
| Envío de Datos a Altamira/Bancomer México (RDR_ALTAMIRAMEX_SEND) | pablo.llorente | 2026-09-18 | salidas/envio_altamira_bancomer_mexico/ — **2026-09-24:** duplicidad de ALID entre sucursales cerrada con código fuente real de `Querys.obtenerIDs()`: confirmado que el `DISTINCT` opera sobre la cadena `LISTAGG` agregada por sucursal, no sobre el ALID individual — la duplicidad entre sucursales es estructuralmente posible, no impedida por diseño (TC-005 actualizado). Segundo hallazgo (RG-7): la misma agregación `LISTAGG` puede concatenar varios ALID en una única línea si una combinación institución/sucursal tiene más de un código asociado, rompiendo la estructura de "un código por línea" (TC-007 actualizado). Riesgos abiertos restantes: cierre asimétrico de cadena (prioridad alta) y discrepancia de conciliación |
| RDR_BANCARIZACION_new (1/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-21 | salidas/rdr_bancarizacion/ — **2026-09-24:** naming de `MEKYTL0157` resuelto con ficha real EX-005-03 (sin espacio en ningún nombre, errata del documento fuente); criticidad real del job corregida a C (aviso inmediato). Diccionario de datos del reporte de bancarización resuelto con `select.properties` real (clave `querybancarizacion`) + muestra de producción real (98.905 registros): 9 campos confirmados contra el esquema GoldenSource, 2 de ellos (`ORIGBANC`/`ORIGOFIC`) siempre vacíos en producción; TC-011 añadido. La asunción sobre el consumo de MVP00G200 ante la sobrescritura de BANCARIZA.txt se retiró de la documentación por indicación expresa del usuario (no relevante). Sin pendientes abiertos |
| RDR_CARGA_BAJA_NIVELES_new (2/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | salidas/rdr_carga_baja_niveles/ |
| RDR_DUCO_CPTY (1/2 cadenas de Extracciones hacia DUCO) | pablo.llorente | 2026-09-23 | salidas/rdr_duco_cpty/ — G1 (criticidad de cadena `F`) resuelto a posteriori con ficha real de Control-M: valor real `S` |
| RDR_ExtraccionDUCOMASTERDATA (2/2 cadenas de Extracciones hacia DUCO) | pablo.llorente | 2026-09-23 | salidas/rdr_extraccion_ducomasterdata/ — G4 (criticidad dual "S / C" de MEKYTL1300) resuelto a posteriori con ficha oficial de Control-M: valor real `W` |
| RDR_CLIENTES_CIB_new (3/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | salidas/rdr_clientes_cib/ |
| RDR_BATCH_EMISORES_REFINITIV (1/3 cadenas de Carga multipetición Refinitiv emisores) | pablo.llorente | 2026-09-22 | salidas/rdr_batch_emisores_refinitiv/ — **2026-09-24:** gap G1 (workflows/tablas GoldenSource) cerrado por completo con los 3 workflows reales (`Refinitiv_Request_Response.wkf`, `Refinitiv_Load_Ratings.wkf`, `BBG_Refinitiv_Batch.wkf`): confirma con código la actualización real de `FT_T_FIRT`/`FT_T_RLT1` (marcadores `CALCULATE_REU`/`PENDING_ESB`), el mapeo de ratings entre 5 agencias (S&P/Moody's/Fitch/DBRS/Scope) vía `FT_T_RTNG`/`FT_T_RVXR`/`FT_T_RTVL`, y 2 tablas nuevas no declaradas antes (`FT_T_FRRL`/`FT_T_INCL`). Único resto sin fichero propio: sub-workflow `RDR_UPDATE_REU` y los 2 jars externos invocados (impacto bajo). La coincidencia `id=MULTI` entre esta cadena y `RDR_CARGA_REFINITIV_Multi` también cerrada con los 2 `.properties` reales: es real (no errata), sin riesgo funcional (el destino se decide por `requestType`/`vreqOid`, no por `id`). Sin más pendientes bloqueantes salvo el relanzamiento manual duplicado (TC-007, ya cubierto sin evidencia adicional) |
| RDR_CARGA_REFINITIV_Multi (2/3 cadenas de Carga multipetición Refinitiv emisores) | pablo.llorente | 2026-09-22 | salidas/rdr_carga_refinitiv_multi/ |
| RDR_ISSUES_RE_PRO_new (3/3 cadenas de Carga multipetición Refinitiv emisores, 30 jobs) | pablo.llorente | 2026-09-22 | salidas/rdr_issues_re_pro_new/ — **2026-09-24:** algoritmo de `RDR_Validacion_XSD.sh` (gap G7) confirmado con script real; nuevo hallazgo R20/TC-021: la validación XSD nunca hace fallar el job, solo el chequeo estructural previo. Diccionario de datos de los 3 XML y vinculación cadena↔XSLT siguen sin confirmar |
| RDR_BBVACONTRACTS_new (Cesión de Contratos BBVA / Legal Agreements) | pablo.llorente | 2026-09-22 | `salidas/cesion_contratos_bbva/` → `spec.md` + `prerrequisitos.md` + `casos_prueba.xml` (19 TC). Alcance reducido a 1 cadena: `_M`, `_L`, `RESPUESTA_MENTOR_LA_BBVA_M`, `DIF_MENTOR_BBVA_new` y el destino Mentor están DECOMISADOS. **2026-09-24, con export real de Control-M y 3 fichas EX-005-03:** ventana de `FW_BBVAContracts_RDR_1` confirmada (14:00-15:30); flag "Force OK" en `VALIDACION_XSD_EXTRACT_BBVA` confirmado activo (el XSD no bloquea nada, corrige respuesta previa); inventario de jobs activos cerrado con hallazgo — 3 ramas de distribución reales (`MEKYTL1246`/GMIP, `MEKYTL1264`/THOR, `MEKYTL1307`/PXVA) se habían asumido erróneamente como decomisadas; job que genera el CSV identificado (`MEKYTL0895` mismo). Cadena ahora de **8 ramas de distribución**, no 6. Generación de Mentor reconfirmada como decomisada específicamente para esta cadena (RG-03 cerrado). `MEKYTL1053` (purga) resuelto como probable decomisión (ficha con IP fija antigua y sin periodicidad, RG-13 cerrado). Único pendiente restante: entornos de prueba (aplazado por el usuario) |
| RDR_EXTRACCION_CONTACTOS (Extracción Genérica de Contactos) | pablo.llorente | 2026-09-22 | `salidas/extraccion_contactos/` → `spec.md` + `prerrequisitos.md` + `casos_prueba.xml` (17 TC). Cadena de 9 jobs L-V, dos consumidores: **IHS Markit** (universo completo, vía disponibilización en DataX) y SAIT (**solo México**, filtro de sait.xsl). Hallazgo principal: R-21 prohíbe el fichero vacío pero ningún control lo impide (RG-02, TC-10 se espera que falle). Segundo hallazgo: `rownum=1` sin `ORDER BY` en `AgreementsAssociated` pierde acuerdos y puede dejar fuera de SAIT a un contacto con acuerdo 1145 (RG-15, TC-17). **2026-09-24:** exclusión `A15` resuelta con query real (`A15`=COMPASS, venta a PNC en 2020); mecanismo de limpieza en pasarela `lpftp503` resuelto con fichas reales (sobrescritura por nombre fijo, RG-05 cerrado); `ArgJava3=20` confirmado por completo (`.properties` + código real de `Ppal.java`: es `NUM_THREADS`). RG-19 cerrado con código real de `Querys.java`: `FT_T_ATE1` no se filtra por `DATA_STAT_TYP` en ningún `SELECT` por `ACTION_NME`, así que el `INACTIVE` de `ExtraccionCONT.sql` (desde 15-SEP-25) es funcionalmente inerte. Único pendiente restante, de bajo impacto: motivo de diseño de la asimetría A15/`DATA_STAT_TYP` (pregunta de intención, no verificable por código) |
| RDR_CONCILIACION_BDI_new (4/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | `salidas/rdr_conciliacion_bdi/` → `spec.md` + `casos_prueba.xml` (7 TC) + `prerrequisitos.md`. 7 jobs lineales; único gap (informe SWIFT sin canal de distribución) confirmado por código fuente (`informeBroker_BDI.wkf`) |
| RDR_CONCILIACION_CLIENTELA_new (5/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | `salidas/rdr_conciliacion_clientela/` → `spec.md` + `casos_prueba.xml` (6 TC) + `prerrequisitos.md`. 4 jobs lineales; única cadena de P-021 con dependencia de entrada real hacia otra cadena del propio sistema (`RDR_REFUNDICION_new`, auto-confirmada por referencia cruzada); gap de alerta ante ausencia de fichero y gap transversal de criticidad "W / S / C" resueltos |
| RDR_ENVIO_CLIEX_new (6/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | `salidas/rdr_envio_cliex/` → `spec.md` + `casos_prueba.xml` (7 TC) + `prerrequisitos.md`. 8 jobs con Fan-In real (2 historificaciones); asimetría de tolerancia confirmada entre MEKYTL0783 (estricto) y MEKYTL0784 (Soft Failure); `CLIEXCLU.csv` confirmado como artefacto interno nunca transmitido por XCOM. **2026-09-24:** mecanismo interno de `MEKYTL0783` (G1) cerrado con el script real `MEGENV0001.sh` — `FALLA_NO_FICHERO="SI"` + `GetExitCode 60` confirmado, genérico para todo el motor de envíos, no exclusivo de este job. Sin pendientes bloqueantes |
| RDR_PR_BDICLIENREG_RESP_new (7/8 cadenas del sistema P-021) | pablo.llorente | 2026-09-23 | `salidas/rdr_pr_bdiclienreg_resp/` → `spec.md` + `casos_prueba.xml` (6 TC) + `prerrequisitos.md`. Cadena cíclica de 10 jobs (disparo cada 5 min, sin Fan-Out/Fan-In); doble control de concurrencia confirmado (lock externo `controlSCF.txt` gestionado por SCF/Investors Plan + fichero `ACKNACK_*.txt`); gap transversal de criticidad resuelto |
| RDR_REFUNDICION_new (8/8 cadenas del sistema P-021 — sistema P-021 completo) | pablo.llorente | 2026-09-23 | `salidas/rdr_refundicion/` → `spec.md` + `casos_prueba.xml` (6 TC) + `prerrequisitos.md`. 4 jobs + Fan-Out real de salida hacia `RDR_CONCILIACION_CLIENTELA_new` (auto-confirmado por referencia cruzada en el documento fuente, sin pregunta); motor GoldenSource `PLSQL_Load` reutilizado; gap transversal de criticidad resuelto. **Con esta cadena se completan las 8 cadenas de P-021 (48 pasos)** |

## 5. Supuestos y decisiones pendientes de confirmación
> Hipótesis de simulacro o pendientes de confirmar por un usuario. No deben usarse como
> respuestas reutilizables (sección 2) hasta que se confirmen literalmente en sesión.

| Supuesto | Proceso | Usuario | Estado | Comentario |
|----------|---------|---------|--------|------------|
| Cierre asimétrico de cadena (MEKYTL1221 no conectado a RDR_ALTAMIRAMEX_SEND_OUT) | Envío a Altamira/Bancomer México | pablo.llorente | Riesgo confirmado, no resuelto | Control-M puede marcar la cadena como completada con éxito aunque la transmisión a DataX falle; prioridad alta antes de confiar en el estado de Control-M como indicador de éxito real |
| Entornos de ejecución de pruebas | RDR_BBVACONTRACTS_new | pablo.llorente | Aplazado por el usuario | Decisión expresa de continuar sin definirlos |
| Vinculación exacta cadena↔plantilla XSLT (`Extraccion_Emisiones.xsl`) | RDR_ISSUES_RE_PRO_new | pablo.llorente | No confirmado (decisión explícita del usuario) | El propio usuario indicó dejarlo como no confirmado al no existir evidencia de invocación específica — no forzar el cierre |
| Comportamiento ante relanzamiento manual duplicado (mismo vreqOid / mismo emisiones.xml el mismo ODATE) | RDR_BATCH_EMISORES_REFINITIV / RDR_ISSUES_RE_PRO_new | pablo.llorente | Pendiente de entorno de prueba | Sin control de duplicidad documentado a nivel de Control-M; tratado como observación a registrar, no como comportamiento asumido, en TC-007 de ambos procesos |
| Si es deliberado que el `NOT EXISTS` de la exclusión `A15` no filtre por `CNTA.DATA_STAT_TYP` (RG-06) | RDR_EXTRACCION_CONTACTOS | pablo.llorente | Pendiente (pregunta de intención de diseño, no verificable por código) | La identidad (`A15`=COMPASS) y el motivo de negocio (venta a PNC en 2020) ya están confirmados; los metadatos de auditoría de `FT_T_ATE1` (`LAST_CHG_USR_ID`/`LAST_CHG_TMS`) no revelan intención de diseño, solo quién tocó la query y cuándo |
